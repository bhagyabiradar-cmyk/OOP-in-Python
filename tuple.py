# Creating a tuple
numbers = (10, 20, 30, 40)

# Accessing elements
print("First number:", numbers[0])

# Length of tuple
print("Length:", len(numbers))

# Iterating through tuple
for num in numbers:
    print(num)